services:
  - type: web
    name: causal-playground
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn causal_playground.app_dash.app:server --workers 2 --threads 4 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.12
